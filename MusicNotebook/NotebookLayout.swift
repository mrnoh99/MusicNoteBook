//
//  NotebookLayout.swift
//  MusicNotebook
//

import CoreGraphics

/// Shared page geometry used by the editor canvas and page thumbnails so both render identically.
enum NotebookLayout {
    static let pageSize = CGSize(width: 1000, height: 1400)
}
